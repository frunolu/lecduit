# lecduit

## 💾 Import databázy
Pre rýchle nastavenie databázy:
1. Skontrolujte prihlasovacie údaje v `Database.php`.
2. Uistite sa, že na serveri je prítomný súbor `database.sql`.
3. Spustite skript `import_db.php` cez prehliadač.
4. **Ihneď po importe zmažte `import_db.php` a `database.sql`!**

